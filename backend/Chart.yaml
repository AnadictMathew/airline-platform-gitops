# Helm chart basic information
apiVersion: v2
name: airline-backend
description: Backend service for airline app
type: application

version: 0.1.0         # Chart version
appVersion: "1.0"      # App version (not docker tag)